apiVersion: v1
kind: Service
metadata:
  name: status-page-service
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8000
  - name: app-port
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: status-page
  type: LoadBalancer
